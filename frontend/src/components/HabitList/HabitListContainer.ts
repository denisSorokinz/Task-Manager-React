import styled from "styled-components";

const HabitsListContainer = styled.ul`
    display: flex;
    align-items: center;
    overflow-x: scroll;
    width: 90vw;
    margin: 1.75rem auto 0;
    padding: 0.5rem 0;
`;

export default HabitsListContainer;